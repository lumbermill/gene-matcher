

desc 'Run test'
task :test do
  puts "TODO"
end

namespace :gem do
  desc 'Build gem'
  task :build do
    puts `gem build gene-matcher.gemspec`
    puts "`gem push gene-matcher-0.x.x.gem` to publish the gem."
    puts "See https://guides.rubygems.org/make-your-own-gem/ for details."
  end
end
